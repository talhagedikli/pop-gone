/// @description Insert description here
// You can write your code in this editor
draw_self();

if (showText)
{
	element
	.wrap(width, height)
	.align(fa_left, fa_top)
	.msdf_shadow(c_dkgray, 0.6, 2, 1)
	.draw(bbox_left + margin, bbox_top + margin);
	
	//show(s.get_typewriter_state());
	
}